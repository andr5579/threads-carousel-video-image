# threads-carousel-video-image
Automated carousel publishing in Threads (video + image together) every 2 hours: selects a pending post from Google Sheets, downloads media from Google Drive, uploads via Cloudinary, creates media containers and publishes the carousel through the Threads API, updates post status and sends a Telegram notification.
